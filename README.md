# AUTOPASS

[![pipeline status](https://gitlab.crja72.ru/django/2025/autumn/course/projects/team-9/badges/main/pipeline.svg)](https://gitlab.crja72.ru/django/2025/autumn/course/projects/team-9/pipelines)

## 📖 О проекте

### 🛠️ Как запустить проект в dev-режиме?

1. **КЛОНИРОВАНИЕ РЕПРЕЗИТОРИЯ**
    Клонируем проект с GitLab:

    ``` bash
    git clone https://gitlab.crja72.ru/django/2025/autumn/course/projects/team-9.git
    ```

2. **ПРОВЕРКА ВЕРСИИ PYTHON**

    Проект требует Python 3.8 или выше
    Проверить версию можно командой:

    ``` bash
    python --version
    ```

    *Если версия ниже 3.8, установите более новую версию Python*

3. **НАСТРОЙКА ОКРУЖЕНИЯ**

    Создаем виртуальное окружение

    ``` bash
    python -m venv venv
    ```

    Активируем виртуальное окружение

    ``` bash
    source venv/bin/activate
    ```

4. **КОНФИГУРАЦИЯ ПРОЕКТА**

    устанавливаем зависимости

    ``` bash
    pip install -r requirements/dev.txt
    ```

    Копируем файл окружения

    ``` bash
    cp .env.example .env
    ```

    Редактируем .env файл:

    ``` Python3
    DEBUG=True
    ```

5. **ЗАПУСК ПРОЕКТА**

    Переходим в папку с проектом (если нужно)

    ``` bash
    cd autopass
    ```

    Запускаем сервер разработки

    ``` bash
    python manage.py runserver
    ```

    Сервер будет доступен по адресу: *<http://127.0.0.1:8000/>*

### СХЕМА БАЗЫ ДАННЫХ

![Схема БД](ER.jpg)
